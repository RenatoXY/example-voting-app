name: CI-CD Pipeline DevOps

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del codigo
        uses: actions/checkout@v3

      - name: Construir Imagen Docker
        run: docker build -t mi-microservicio ./vote

      - name: Ejecutar Pruebas Unitarias
      # IE2: Integra pruebas para garantizar estabilidad [cite: 11]
        run: echo "Ejecutando pruebas..."

      - name: Escaneo de Seguridad
      # IE3: Ajusta parámetros de seguridad dentro del pipeline [cite: 12]
        run: echo "Analizando seguridad..."

      - name: Despliegue Simulado
      # IE4: Despliega automáticamente garantizando trazabilidad [cite: 13]
        run: echo "Desplegando app..."
